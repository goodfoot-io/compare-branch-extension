---
name: card-repo
description: Card repository reference
---

The card repository reference moved to [card-repo.md](/workspace/public/packages/claude-code-hooks-runtime/src/content/card-repo.md).

Session and subagent hooks now prepend that markdown to their `systemMessage` and `additionalContext` output so the runtime and the skill share one source of truth.
