/**
 * Tests for Cards CLI binary source files — card management, comparison,
 * notifications, and Puppeteer-based webview interaction.
 *
 * Each test file covers the pure helper functions of its corresponding binary.
 * Puppeteer integration tests are excluded as they require a running VS Code instance.
 *
 * @summary Test suite barrel for Cards CLI binary tests
 */

export {};
