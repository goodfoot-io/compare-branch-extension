/**
 * Test suite for the default-configuration package.
 *
 * Covers action handlers (chat, codex, interview, launch), worktree lifecycle,
 * and error propagation scenarios.
 *
 * @summary Integration and unit tests for built-in card actions and stream transforms
 */

export {};
