/**
 * Built-in card action handlers.
 *
 * Actions are compiled to standalone binaries by the @cards.management/sdk build system
 * and invoked at runtime. They have no importable runtime API.
 *
 * @summary Built-in card actions (chat, interview, launch)
 */

export {};
