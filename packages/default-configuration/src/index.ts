// Handlers are compiled to standalone binaries by @cards.management/sdk/config build system.
// This package does not export library functions.

/**
 * Default card actions and stream transforms shipped with
 * the extension. All handlers are compiled to standalone binaries at build
 * time and have no importable runtime API.
 *
 * @summary Built-in actions and stream transforms for the Cards extension
 */

export {};
