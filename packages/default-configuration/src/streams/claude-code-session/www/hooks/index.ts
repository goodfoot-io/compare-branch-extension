/**
 * Barrel export for hooks used by the claude-code-session renderer.
 *
 * @summary Hooks barrel export
 * @module hooks/index
 */

export { useStreamStore } from './useStreamStore';
export { useThemeAttribute } from './useThemeAttribute';
