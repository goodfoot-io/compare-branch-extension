{
  "name": "@cards/claude-code-hooks-assistant",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node -e \"for(const p of ['../../claude/cards-assistant/hooks','../../claude/cards-assistant/bin','../../claude/cards-assistant/content'])require('fs').rmSync(p,{recursive:true,force:true})\" && claude-code-hooks -i \"src/**/*.ts\" -o \"../../claude/cards-assistant/hooks/hooks.json\" --loader .md=text --loader .txt=text --log-env-var CLAUDE_CODE_ASSISTANT_HOOKS_LOG_FILE --executable \"\\\"\\$(cat \\$HOME/.cards/VSCODE_NODE 2>/dev/null || echo node)\\\"\"",
    "watch": "yarn build && chokidar 'src/**/*.{ts,md,txt}' -c 'yarn build'",
    "test": "vitest-unchanged",
    "lint": "biome check --write --unsafe . && jsdoczoom -l",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@goodfoot/claude-code-hooks": "^1.7.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@cards/test-utils": "workspace:*",
    "@types/node": "24.x",
    "@typescript/native-preview": "^7.0.0-dev",
    "chokidar-cli": "^3.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=22"
  }
}
