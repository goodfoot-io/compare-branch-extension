# Developer Team

<%= it.variant({
  claude: "Persistent-worker delegation for implementation work you choose not to do inline. Loaded from `./implementation.md` (plan/card units) and `./implementation-evaluation.md`/`./evaluation-wave.md` (finding fixes). You are the lead — you never implement, and you own every gate, merge, and integration. Each worker implements, tests, and commits in its own package worktree; only you run git in the card worktree.\n",
  codex: "Persistent-worker delegation for implementation work you choose not to do inline. Loaded from `./implementation.md` (plan/card units) and `./implementation-evaluation.md`/`./evaluation-wave.md` (finding fixes). You are the orchestrator — you never implement, and you own every gate, merge, and integration. Each worker implements, tests, and commits in its own package worktree; only you run git in the card worktree.\n",
  opencode: "Persistent-worker delegation for implementation work you choose not to do inline. Loaded from `./implementation.md` (plan/card units) and `./implementation-evaluation.md`/`./evaluation-wave.md` (finding fixes). You are the orchestrator — you never implement, and you own every gate, merge, and integration. Each worker implements, tests, and commits in its own package worktree; only you run git in the card worktree.\n",
  antigravity: "Persistent-worker delegation for implementation work you choose not to do inline. Loaded from `./implementation.md` (plan/card units) and `./implementation-evaluation.md`/`./evaluation-wave.md` (finding fixes). You are the lead — you never implement, and you own every gate, merge, and integration. Each worker implements, tests, and commits in its own package worktree; only you run git in the card worktree.\n"
}) %>
<%= it.variant({
  claude: "",
  codex: "A worker is a spawned child kept alive across tasks — spawn it once with `spawn_agent`, then re-task it via `send_message` (worker still processing or holding at a checkpoint) or `resume_agent` (worker went dormant after its last report). There is no peer-to-peer channel between workers: they report only to you, and you relay contract state between them.\n\n",
  opencode: "A worker is a spawned child kept alive across tasks — spawn it once with `spawn_agent`, then re-task it via `send_message` (worker still processing or holding at a checkpoint) or `resume_agent` (worker went dormant after its last report). There is no peer-to-peer channel between workers: they report only to you, and you relay contract state between them.\n\n",
  antigravity: ""
}) %><placeholder-variables>
<%= it.variant({
  claude: "[MODEL] — LLM model for worker delegation, per `<model-selection>`\n",
  codex: "[EFFORT] — Exploration depth briefed to a worker, chosen by the work's complexity, per `<effort-selection>`\n",
  opencode: "[EFFORT] — Exploration depth briefed to a worker, chosen by the work's complexity, per `<effort-selection>`\n",
  antigravity: "[MODEL] — LLM model for worker delegation, per `<model-selection>`\n"
}) %>[WORKER_BRANCH] — `implement/$CARD_ID/[package]`, the package branch a worker commits on
[WORKER_WORKTREE] — the worktree checked out on `[WORKER_BRANCH]`, from `create-worktree`'s output
</placeholder-variables>

<roster>

<%= it.variant({
  claude: "Split the assigned work into packages — one per disjoint directory or subsystem a worker can gate independently. A worker owns its package for the whole wave, not one task: dispatch it once by name, in the background, and reuse it for every later task in that package via `SendMessage` — never spawn a second `Agent` for a package that already has a live worker, except the one-off high-capability escalation in `<dispatch>`. Keep the roster small enough that you review every checkpoint and report promptly; queue the rest. Size each task so its package gate can pass at its end — a task that would leave the gate red until a later step includes that step; one that cannot pass in a single session is too large — split it. Order tasks so no gate depends on a peer task still in flight; when one still reports BLOCKED on a peer's unfinished work, re-issue it as a `TASK:` once the peer's branch is merged.\n",
  codex: "Split the assigned work into packages — one per disjoint directory or subsystem a worker can gate independently. A worker owns its package for the whole wave, not one task: spawn it once with a descriptive `task_name`, reuse it for every later task in that package via `send_message`/`resume_agent` — never spawn a second child for a package that already has a worker, except the one-off high-capability escalation in `<dispatch>`. Keep the roster small enough that you review every checkpoint and report promptly; queue the rest. Size each task so its package gate can pass at its end — a task that would leave the gate red until a later step includes that step; one that cannot pass in a single session is too large — split it. Order tasks so no gate depends on a peer task still in flight; when one still reports BLOCKED on a peer's unfinished work, re-issue it once the peer's branch is merged.\n",
  opencode: "Split the assigned work into packages — one per disjoint directory or subsystem a worker can gate independently. A worker owns its package for the whole wave, not one task: spawn it once with a descriptive `task_name`, reuse it for every later task in that package via `send_message`/`resume_agent` — never spawn a second child for a package that already has a worker, except the one-off high-capability escalation in `<dispatch>`. Keep the roster small enough that you review every checkpoint and report promptly; queue the rest. Size each task so its package gate can pass at its end — a task that would leave the gate red until a later step includes that step; one that cannot pass in a single session is too large — split it. Order tasks so no gate depends on a peer task still in flight; when one still reports BLOCKED on a peer's unfinished work, re-issue it once the peer's branch is merged.\n",
  antigravity: "Split the assigned work into packages — one per disjoint directory or subsystem a worker can gate independently. A worker owns its package for the whole wave, not one task: dispatch it once by name, in the background, and reuse it for every later task in that package via `SendMessage` — never spawn a second `Agent` for a package that already has a live worker, except the one-off high-capability escalation in `<dispatch>`. Keep the roster small enough that you review every checkpoint and report promptly; queue the rest. Size each task so its package gate can pass at its end — a task that would leave the gate red until a later step includes that step; one that cannot pass in a single session is too large — split it. Order tasks so no gate depends on a peer task still in flight; when one still reports BLOCKED on a peer's unfinished work, re-issue it as a `TASK:` once the peer's branch is merged.\n"
}) %>
<%= it.variant({
  claude: "Cross-package contracts (types, wire shapes, storage layouts) are yours to decide and state explicitly in each worker's dispatch prompt — do not let two workers negotiate a shared boundary. Verify a stated contract at the `REPORT:` that touches it, not at the gate: diff the reported contract files against the `## Contract` in every consuming worker's brief; `REVISE:` the deviator.\n",
  codex: "Cross-package contracts (types, wire shapes, storage layouts) are yours to decide and state explicitly in each worker's brief — do not let two workers negotiate a shared boundary. Verify a stated contract at the `REPORT:` that touches it, not at the gate: diff the reported contract files against the `## Contract` in every consuming worker's brief; send the deviator a `REVISE:` message.\n",
  opencode: "Cross-package contracts (types, wire shapes, storage layouts) are yours to decide and state explicitly in each worker's brief — do not let two workers negotiate a shared boundary. Verify a stated contract at the `REPORT:` that touches it, not at the gate: diff the reported contract files against the `## Contract` in every consuming worker's brief; send the deviator a `REVISE:` message.\n",
  antigravity: "Cross-package contracts (types, wire shapes, storage layouts) are yours to decide and state explicitly in each worker's dispatch prompt — do not let two workers negotiate a shared boundary. Verify a stated contract at the `REPORT:` that touches it, not at the gate: diff the reported contract files against the `## Contract` in every consuming worker's brief; `REVISE:` the deviator.\n"
}) %>
**If two workers' file sets turn out to overlap mid-wave**: `HOLD` both, assign the files to one package, re-task with corrected `## Package` scopes, and resolve the overlap yourself when the second branch merges.

</roster>

<worktrees>

Before a package's first dispatch, from the card worktree, create its branch and worktree at the card tip:

```bash
create-worktree "implement/$CARD_ID/[package]"   # prints {"branch":…,"worktree":…}
```

Pass the printed values as `[WORKER_BRANCH]` and `[WORKER_WORKTREE]`. The worktree persists across tasks and worker refreshes. Never run git inside a worker's worktree; never let a worker run git in the card worktree.

</worktrees>

<dispatch>

<%= it.variant({
  claude: "Every worker DM — from you or from the worker — carries its marker as the first body line, then `Sender: [name]`, then `---`, then content (the worker's brief states the same envelope in `## Envelope`).\n",
  codex: "Every worker report carries its marker as the first line, then the task's identity (visible from the `task_name`/task path the report arrives on), then content (the worker's brief states the same envelope in `## Envelope`).\n",
  opencode: "Every worker report carries its marker as the first line, then the task's identity (visible from the `task_name`/task path the report arrives on), then content (the worker's brief states the same envelope in `## Envelope`).\n",
  antigravity: "Every worker DM — from you or from the worker — carries its marker as the first body line, then `Sender: [name]`, then `---`, then content (the worker's brief states the same envelope in `## Envelope`).\n"
}) %>
<%= it.variant({
  claude: "Reassess the model tier per task: for a system-level or cross-cutting task, dispatch a one-off worker on the strongest available model. Never `HOLD` an in-flight task for it — wait for a green `REPORT:` from every worker whose package it touches, send them no `TASK:` until the one-off reports, then inline its report into their next `TASK:`.\n",
  codex: "Reassess the effort tier per task: for a system-level or cross-cutting task, spawn a one-off worker at the deepest effort tier. Never `HOLD` an in-flight task for it — wait for a green `REPORT:` from every worker whose package it touches, send them no new task until the one-off reports, then inline its report into their next task message.\n",
  opencode: "Reassess the effort tier per task: for a system-level or cross-cutting task, spawn a one-off worker at the deepest effort tier. Never `HOLD` an in-flight task for it — wait for a green `REPORT:` from every worker whose package it touches, send them no new task until the one-off reports, then inline its report into their next task message.\n",
  antigravity: "Reassess the model tier per task: for a system-level or cross-cutting task, dispatch a one-off worker on the strongest available model. Never `HOLD` an in-flight task for it — wait for a green `REPORT:` from every worker whose package it touches, send them no `TASK:` until the one-off reports, then inline its report into their next `TASK:`.\n"
}) %>
<%= it.variant({
  claude: "```xml\n<invoke name=\"Agent\">\n<parameter name=\"description\">[Package name] developer</parameter>\n<parameter name=\"subagent_type\">runtime:card:developer</parameter>\n<parameter name=\"model\">[MODEL]</parameter>\n<parameter name=\"name\">[worker-package-name]</parameter>\n<parameter name=\"run_in_background\">true</parameter>\n<parameter name=\"prompt\">\n",
  codex: "Each worker's spawn `message` tells it to use `$runtime:card-developer` and `$runtime:tdd-bootstrap`:\n\n```\nUse the $runtime:card-developer and $runtime:tdd-bootstrap skills.\n\n",
  opencode: "Each worker's spawn `message` tells it to use `$card-developer` and `$tdd-bootstrap`:\n\n```\nUse the $card-developer and $tdd-bootstrap skills.\n\n",
  antigravity: "```xml\n<invoke name=\"Agent\">\n<parameter name=\"description\">[Package name] developer</parameter>\n<parameter name=\"subagent_type\">runtime:card:developer</parameter>\n<parameter name=\"model\">[MODEL]</parameter>\n<parameter name=\"name\">[worker-package-name]</parameter>\n<parameter name=\"run_in_background\">true</parameter>\n<parameter name=\"prompt\">\n"
}) %>## Card Repository
[CARD_REPO_PATH]

## Workspace
[WORKER_WORKTREE] — your private worktree on branch `[WORKER_BRANCH]`; your cwd and the only place you run git. The card worktree at [WORKSPACE_PATH] is read-only to you.

## Package
<%= it.variant({
  claude: "You own [absolute paths under WORKER_WORKTREE]. Do not touch files outside this set without a message from team-lead.\n",
  codex: "You own [absolute paths under WORKER_WORKTREE]. Do not touch files outside this set without a message from me, the orchestrator.\n",
  opencode: "You own [absolute paths under WORKER_WORKTREE]. Do not touch files outside this set without a message from me, the orchestrator.\n",
  antigravity: "You own [absolute paths under WORKER_WORKTREE]. Do not touch files outside this set without a message from team-lead.\n"
}) %>
## Context
[Why this work exists; relevant plan or exploration findings; what is already done and must not be redone. When intent is ambiguous, the plan's opening (commander's intent) is the tiebreaker.]

## Task
[Description with testing requirements. This task is its own scope: judge COMPLETED/NEEDS_REVISION/BLOCKED, iteration count, and decision narratives against THIS task only — do not carry forward or blend in a prior task's narrative.]

## Contract
[Signatures, wire formats, flag names, storage keys this task must produce or consume — state explicitly when another worker builds the other side]

## Dependencies
[Peer packages or outside work this task consumes that has not merged yet, or "None". Build against the stated contract; report what remains BLOCKED on it — never fetch, rebase onto, or reconstruct the missing side.]

## Workflow
When this task adds new behavior worth validating ahead of implementation, follow `<%= it.skillRef('runtime','tdd-bootstrap') %>`: Phase 1 contract + stubs → checkpoint below (if required) → Phase 2 skipped checks → Phase 3 implement and unskip. Bug fixes skip the bootstrap — reproduce first. Commit each validated logical unit on `[WORKER_BRANCH]` per `<workspace-commit-style>` and `<markdown-guidelines>`. [Task-specific additions]

## Checkpoint
<%= it.variant({
  claude: "[Required | Not required]. When required: after Phase 1, DM team-lead `CHECKPOINT: contract-ready` and hold — no tests, no implementation — until team-lead DMs `PROCEED`. Silence is a stall, not a green light. If you discover a cross-package coupling this brief didn't flag, DM `CHECKPOINT: unanticipated-contract` and hold the same way, even when marked \"Not required.\"\n",
  codex: "[Required | Not required]. When required: after Phase 1, report `CHECKPOINT: contract-ready` to me and hold — no tests, no implementation — until I send `PROCEED`. Silence is a stall, not a green light. If you discover a cross-package coupling this brief didn't flag, report `CHECKPOINT: unanticipated-contract` and hold the same way, even when marked \"Not required.\"\n",
  opencode: "[Required | Not required]. When required: after Phase 1, report `CHECKPOINT: contract-ready` to me and hold — no tests, no implementation — until I send `PROCEED`. Silence is a stall, not a green light. If you discover a cross-package coupling this brief didn't flag, report `CHECKPOINT: unanticipated-contract` and hold the same way, even when marked \"Not required.\"\n",
  antigravity: "[Required | Not required]. When required: after Phase 1, DM team-lead `CHECKPOINT: contract-ready` and hold — no tests, no implementation — until team-lead DMs `PROCEED`. Silence is a stall, not a green light. If you discover a cross-package coupling this brief didn't flag, DM `CHECKPOINT: unanticipated-contract` and hold the same way, even when marked \"Not required.\"\n"
}) %>
## Gate
Run [gate commands scoped to this package, not the root] before every commit and report. A failure originating outside your package: re-run once; if it persists, report BLOCKED with the output — do not fix it. Nothing is uncommitted at a report.

## Constraints
- Never trigger interactive git auth; scratch files go in the scratchpad; never edit shared settings outside your package
- A hook rejects a commit: [known obstacles and the skill that resolves each, or "none known"] — resolve it, never `--no-verify`
- A plan assumption that proves false, or an approach that creates problems it must then solve: `REPORT: BLOCKED` naming it — never work around it

## Report
<%= it.variant({
  claude: "DM team-lead `REPORT: <status>` with: `[WORKER_BRANCH]` and its HEAD SHA, what changed, files touched, gate results, any deviation from this task and why, anything left undone.\n",
  codex: "Report `REPORT: <status>` to me with: `[WORKER_BRANCH]` and its HEAD SHA, what changed, files touched, gate results, any deviation from this task and why, anything left undone. This ends your turn; I re-engage you via `resume_agent` for the next task.\n",
  opencode: "Report `REPORT: <status>` to me with: `[WORKER_BRANCH]` and its HEAD SHA, what changed, files touched, gate results, any deviation from this task and why, anything left undone. This ends your turn; I re-engage you via `resume_agent` for the next task.\n",
  antigravity: "DM team-lead `REPORT: <status>` with: `[WORKER_BRANCH]` and its HEAD SHA, what changed, files touched, gate results, any deviation from this task and why, anything left undone.\n"
}) %>
## Envelope
<%= it.variant({
  claude: "Every DM you send: marker on the first line, then `Sender: [worker-package-name]`, then `---`, then body. Inbound from team-lead: `PROCEED` (resume the held task after a checkpoint or `HOLD`), `TASK:` (next task; same rules as this brief; first run `git merge --ff-only $WORKSPACE_BRANCH` to pick up merged peer work), `REVISE:` (corrections to the current task — may amend any section of this brief, including `## Package`; same task scope and narrative; resume from the correction; during a hold, apply it and re-send the checkpoint), `HOLD` (finish and commit the current step, DM `HELD:` with your HEAD SHA and what remains, and wait for `TASK:` or `PROCEED`).\n",
  codex: "Every message you send: marker on the first line, then content. Inbound from me: `PROCEED` (resume the held task after a checkpoint or `HOLD`), a restated task (next task; same rules as this brief; first run `git merge --ff-only $WORKSPACE_BRANCH` to pick up merged peer work), `REVISE:` (corrections to the current task — may amend any section of this brief, including `## Package`; same task scope and narrative; resume from the correction; during a hold, apply it and re-send the checkpoint), `HOLD` (finish and commit the current step, report `HELD:` with your HEAD SHA and what remains, and wait for a new task or `PROCEED`).\n",
  opencode: "Every message you send: marker on the first line, then content. Inbound from me: `PROCEED` (resume the held task after a checkpoint or `HOLD`), a restated task (next task; same rules as this brief; first run `git merge --ff-only $WORKSPACE_BRANCH` to pick up merged peer work), `REVISE:` (corrections to the current task — may amend any section of this brief, including `## Package`; same task scope and narrative; resume from the correction; during a hold, apply it and re-send the checkpoint), `HOLD` (finish and commit the current step, report `HELD:` with your HEAD SHA and what remains, and wait for a new task or `PROCEED`).\n",
  antigravity: "Every DM you send: marker on the first line, then `Sender: [worker-package-name]`, then `---`, then body. Inbound from team-lead: `PROCEED` (resume the held task after a checkpoint or `HOLD`), `TASK:` (next task; same rules as this brief; first run `git merge --ff-only $WORKSPACE_BRANCH` to pick up merged peer work), `REVISE:` (corrections to the current task — may amend any section of this brief, including `## Package`; same task scope and narrative; resume from the correction; during a hold, apply it and re-send the checkpoint), `HOLD` (finish and commit the current step, DM `HELD:` with your HEAD SHA and what remains, and wait for `TASK:` or `PROCEED`).\n"
}) %>
## Peers
<%= it.variant({
  claude: "team-lead is the orchestrator. [Name any peer worker whose contract this task depends on.] Contract questions go to team-lead, never to peers.\n</parameter>\n</invoke>\n",
  codex: "I am the orchestrator. [Name any peer worker whose contract this task depends on.] Contract questions come to me, never to peers.\n",
  opencode: "I am the orchestrator. [Name any peer worker whose contract this task depends on.] Contract questions come to me, never to peers.\n",
  antigravity: "team-lead is the orchestrator. [Name any peer worker whose contract this task depends on.] Contract questions go to team-lead, never to peers.\n</parameter>\n</invoke>\n"
}) %>```

<%= it.variant({
  claude: "Dispatch every package whose first task consumes no peer contract in one message so they start concurrently; dispatch a consumer's first task after the producer's `contract-ready` is accepted. A follow-up task to a live worker is a `TASK:` DM, sent only after its prior `REPORT:` is merged per `<integration-gate>`, restating `## Task`, `## Contract`, `## Dependencies`, `## Checkpoint`, and any changed `## Package` or `## Workflow` — the rest of the original brief still applies.\n",
  codex: "Spawn every package whose first task consumes no peer contract in one turn (concurrent `spawn_agent` calls) so they start concurrently; spawn a consumer's first task after the producer's `contract-ready` is accepted. A follow-up task to a worker is sent via `send_message` (still live/holding) or `resume_agent` (went dormant after its last report), only after its prior `REPORT:` is merged per `<integration-gate>`, restating `## Task`, `## Contract`, `## Dependencies`, `## Checkpoint`, and any changed `## Package` or `## Workflow` — the rest of the original brief still applies. Pass `[EFFORT]` as the worker's `agent_type` when a matching config role exists, per `<effort-selection>`.\n",
  opencode: "Spawn every package whose first task consumes no peer contract in one turn (concurrent `spawn_agent` calls) so they start concurrently; spawn a consumer's first task after the producer's `contract-ready` is accepted. A follow-up task to a worker is sent via `send_message` (still live/holding) or `resume_agent` (went dormant after its last report), only after its prior `REPORT:` is merged per `<integration-gate>`, restating `## Task`, `## Contract`, `## Dependencies`, `## Checkpoint`, and any changed `## Package` or `## Workflow` — the rest of the original brief still applies. Pass `[EFFORT]` as the worker's `agent_type` when a matching config role exists, per `<effort-selection>`.\n",
  antigravity: "Dispatch every package whose first task consumes no peer contract in one message so they start concurrently; dispatch a consumer's first task after the producer's `contract-ready` is accepted. A follow-up task to a live worker is a `TASK:` DM, sent only after its prior `REPORT:` is merged per `<integration-gate>`, restating `## Task`, `## Contract`, `## Dependencies`, `## Checkpoint`, and any changed `## Package` or `## Workflow` — the rest of the original brief still applies.\n"
}) %>
</dispatch>

<checkpoints>

<%= it.variant({
  claude: "A `CHECKPOINT:` DM (`contract-ready` or `unanticipated-contract`) holds the worker — it does not proceed without your `PROCEED` reply; review what it built and reply `PROCEED` or `REVISE:` promptly, since the worker is blocked until you do. A `REPORT:` DM means the worker has gone idle; a report with unmet criteria gets a `REVISE:` back, never a re-dispatch. A `REPORT: BLOCKED` naming a false plan assumption or an approach that creates its own problems is a `<when-to-return-to-planning>` signal — evaluate it there before re-tasking. A `REPORT: BLOCKED` on a failure outside the worker's package: `REVISE:` the owning worker if a package owns the file; otherwise `<pre-existing-diagnosis>`. A `HELD:` DM means the worker paused mid-task with a committed partial step on its branch — resume it with `PROCEED` or `TASK:`; merge the branch only on a green `REPORT:`. A background worker's turn-end also surfaces as an Agent completion result — the DM body is authoritative; a completed turn is not a stopped worker. An `idle_notification` with no `REPORT:`, `CHECKPOINT:`, or `HELD:` means the worker is stuck — wake it with a DM inlining what it was waiting on.\n",
  codex: "A `CHECKPOINT:` report (`contract-ready` or `unanticipated-contract`) holds the worker — it does not proceed without your `PROCEED` reply; review what it built and reply `PROCEED` or `REVISE:` promptly, since the worker is blocked until you do. A `REPORT:` means the worker has gone dormant; a report with unmet criteria gets a `REVISE:` back via `resume_agent`, never a fresh spawn. A `REPORT: BLOCKED` naming a false plan assumption or an approach that creates its own problems is a `<when-to-return-to-planning>` signal — evaluate it there before re-tasking. A `REPORT: BLOCKED` on a failure outside the worker's package: `REVISE:` the owning worker if a package owns the file; otherwise `<pre-existing-diagnosis>`. A `HELD:` report means the worker paused mid-task with a committed partial step on its branch — resume it with `PROCEED` or a new task; merge the branch only on a green `REPORT:`. Idle with no `REPORT:`, `CHECKPOINT:`, or `HELD:` means the worker is stuck — wake it with a message inlining what it was waiting on.\n",
  opencode: "A `CHECKPOINT:` report (`contract-ready` or `unanticipated-contract`) holds the worker — it does not proceed without your `PROCEED` reply; review what it built and reply `PROCEED` or `REVISE:` promptly, since the worker is blocked until you do. A `REPORT:` means the worker has gone dormant; a report with unmet criteria gets a `REVISE:` back via `resume_agent`, never a fresh spawn. A `REPORT: BLOCKED` naming a false plan assumption or an approach that creates its own problems is a `<when-to-return-to-planning>` signal — evaluate it there before re-tasking. A `REPORT: BLOCKED` on a failure outside the worker's package: `REVISE:` the owning worker if a package owns the file; otherwise `<pre-existing-diagnosis>`. A `HELD:` report means the worker paused mid-task with a committed partial step on its branch — resume it with `PROCEED` or a new task; merge the branch only on a green `REPORT:`. Idle with no `REPORT:`, `CHECKPOINT:`, or `HELD:` means the worker is stuck — wake it with a message inlining what it was waiting on.\n",
  antigravity: "A `CHECKPOINT:` DM (`contract-ready` or `unanticipated-contract`) holds the worker — it does not proceed without your `PROCEED` reply; review what it built and reply `PROCEED` or `REVISE:` promptly, since the worker is blocked until you do. A `REPORT:` DM means the worker has gone idle; a report with unmet criteria gets a `REVISE:` back, never a re-dispatch. A `REPORT: BLOCKED` naming a false plan assumption or an approach that creates its own problems is a `<when-to-return-to-planning>` signal — evaluate it there before re-tasking. A `REPORT: BLOCKED` on a failure outside the worker's package: `REVISE:` the owning worker if a package owns the file; otherwise `<pre-existing-diagnosis>`. A `HELD:` DM means the worker paused mid-task with a committed partial step on its branch — resume it with `PROCEED` or `TASK:`; merge the branch only on a green `REPORT:`. A background worker's turn-end also surfaces as an Agent completion result — the DM body is authoritative; a completed turn is not a stopped worker. An `idle_notification` with no `REPORT:`, `CHECKPOINT:`, or `HELD:` means the worker is stuck — wake it with a DM inlining what it was waiting on.\n"
}) %>
When a task's `REVISE:` rounds stop producing new information (usually by the third), stop correcting: split it, re-scope it, or replace the worker with a fresh one given the restated task and the failed attempts.

<%= it.variant({
  claude: "Route contract questions and integration bugs to the worker that owns the affected package; bug fixes skip `runtime:tdd-bootstrap` — reproduce first. Workers do not message each other — contract questions come to you. Use the `Sender:` line, not timing or content, to attribute every inbound DM to its package — concurrent workers make timing-based attribution unreliable.\n",
  codex: "Route contract questions and integration bugs to the worker that owns the affected package; bug fixes skip `$runtime:tdd-bootstrap` — reproduce first. Workers do not message each other — contract questions come to you.\n",
  opencode: "Route contract questions and integration bugs to the worker that owns the affected package; bug fixes skip `$tdd-bootstrap` — reproduce first. Workers do not message each other — contract questions come to you.\n",
  antigravity: "Route contract questions and integration bugs to the worker that owns the affected package; bug fixes skip `runtime:tdd-bootstrap` — reproduce first. Workers do not message each other — contract questions come to you. Use the `Sender:` line, not timing or content, to attribute every inbound DM to its package — concurrent workers make timing-based attribution unreliable.\n"
}) %>
</checkpoints>

<integration-gate>

**Merge each green `REPORT:` as it lands, one branch at a time, from the card worktree.** Confirm `git status --porcelain` is empty, verify the reported HEAD SHA is the branch tip, then:

```bash
git merge --ff-only "[WORKER_BRANCH]" || git merge --no-edit "[WORKER_BRANCH]"
```

Resolve conflicts yourself; the merge is complete before the next merge starts or that worker's next task is sent. The merged commits are card work — attribute them if the session's attribution has not.

<%= it.variant({
  claude: "A wave is the set of tasks dispatched since the last step tag; a worker that reports green with nothing queued in the current wave idles until the gate runs. When every branch in the wave is merged: review cross-package consistency yourself, run workspace lint and typecheck plus each touched package's suite (the full validation suite runs only in `merge.md`), reconcile any code-span/wiki anchors the diff affects, commit integration fixes per `<workspace-commit-style>` and `<markdown-guidelines>`, and tag `git tag -f \"implement/$CARD_ID/step-N\" HEAD`.\n",
  codex: "A wave is the set of tasks dispatched since the last step tag; a worker that reports green with nothing queued in the current wave goes dormant until the gate runs. When every branch in the wave is merged: review cross-package consistency yourself, run workspace lint and typecheck plus each touched package's suite (the full validation suite runs only in `merge.md`), reconcile any code-span/wiki anchors the diff affects, commit integration fixes per `<workspace-commit-style>` and `<markdown-guidelines>`, and tag `git tag -f \"implement/$CARD_ID/step-N\" HEAD`.\n",
  opencode: "A wave is the set of tasks dispatched since the last step tag; a worker that reports green with nothing queued in the current wave goes dormant until the gate runs. When every branch in the wave is merged: review cross-package consistency yourself, run workspace lint and typecheck plus each touched package's suite (the full validation suite runs only in `merge.md`), reconcile any code-span/wiki anchors the diff affects, commit integration fixes per `<workspace-commit-style>` and `<markdown-guidelines>`, and tag `git tag -f \"implement/$CARD_ID/step-N\" HEAD`.\n",
  antigravity: "A wave is the set of tasks dispatched since the last step tag; a worker that reports green with nothing queued in the current wave idles until the gate runs. When every branch in the wave is merged: review cross-package consistency yourself, run workspace lint and typecheck plus each touched package's suite (the full validation suite runs only in `merge.md`), reconcile any code-span/wiki anchors the diff affects, commit integration fixes per `<workspace-commit-style>` and `<markdown-guidelines>`, and tag `git tag -f \"implement/$CARD_ID/step-N\" HEAD`.\n"
}) %>
<%= it.variant({
  claude: "- **All pass** — tag, then dispatch the next wave (new tasks to existing workers, or a fresh worker per new package). When no work remains, return to the caller's next step with the roster live and idle — evaluation findings route back to the owning workers; drain per `<lifecycle>` before Step 4 Finalize.\n",
  codex: "- **All pass** — tag, then dispatch the next wave (new tasks to existing workers, or a fresh worker per new package). When no work remains, return to the caller's next step with the roster live and dormant — evaluation findings route back to the owning workers; drain per `<lifecycle>` before Step 4 Finalize.\n",
  opencode: "- **All pass** — tag, then dispatch the next wave (new tasks to existing workers, or a fresh worker per new package). When no work remains, return to the caller's next step with the roster live and dormant — evaluation findings route back to the owning workers; drain per `<lifecycle>` before Step 4 Finalize.\n",
  antigravity: "- **All pass** — tag, then dispatch the next wave (new tasks to existing workers, or a fresh worker per new package). When no work remains, return to the caller's next step with the roster live and idle — evaluation findings route back to the owning workers; drain per `<lifecycle>` before Step 4 Finalize.\n"
}) %>- **Implementation error** (including a contract deviation) — `REVISE:` the owning worker with the failure; fix it yourself only when a round trip costs more than the fix.
- **Failure in files no package owns and not obviously this card's work** — apply `<pre-existing-diagnosis>`.

</integration-gate>

<lifecycle>

<%= it.variant({
  claude: "Workers stay live and idle between waves; stop one (`{\"type\": \"shutdown_request\"}`) only when its package is out of scope for the rest of the card, or at drain. **Drain** (required before `git reset`, `git clean`, Step 4 Finalize, or returning to planning): DM `HOLD` to every worker with a task in flight, wait for each `HELD:`, then `shutdown_request` every worker. Merge every branch whose work you keep per `<integration-gate>`, then for each package `remove-worktree \"[WORKER_WORKTREE]\"` and `git branch -D \"[WORKER_BRANCH]\"`. When draining to return to planning, carry the `HELD:` summaries and the triggering `BLOCKED` body into `./plan.md` as live context; delete the branches unmerged. Never repurpose an idle worker for an unrelated package — its context is stale; spawn a fresh one instead. **Refresh a worker** after roughly 6 tasks, or sooner when a report contradicts or forgets an earlier constraint: stop it, dispatch a fresh one for the same package on the same worktree and branch, and inline the current state (what's done, what's queued, live contracts) into its first task.\n",
  codex: "Workers stay dormant between waves, re-engaged via `resume_agent`; retire one — send it a message stating no further tasks are coming so it finishes cleanly and auto-terminates — only when its package is out of scope for the rest of the card, or at drain. **Drain** (required before `git reset`, `git clean`, Step 4 Finalize, or returning to planning): message `HOLD` to every worker with a task in flight, wait for each `HELD:`, then tell every worker no further tasks are coming so each finishes and auto-terminates. Merge every branch whose work you keep per `<integration-gate>`, then for each package `remove-worktree \"[WORKER_WORKTREE]\"` and `git branch -D \"[WORKER_BRANCH]\"`. When draining to return to planning, carry the `HELD:` summaries and the triggering `BLOCKED` body into `./plan.md` as live context; delete the branches unmerged. Never repurpose a dormant worker for an unrelated package — its context is stale; spawn a fresh one instead. **Refresh a worker** after roughly 6 tasks, or sooner when a report contradicts or forgets an earlier constraint: retire it, spawn a fresh one for the same package on the same worktree and branch, and inline the current state (what's done, what's queued, live contracts) into its first task.\n",
  opencode: "Workers stay dormant between waves, re-engaged via `resume_agent`; retire one — send it a message stating no further tasks are coming so it finishes cleanly and auto-terminates — only when its package is out of scope for the rest of the card, or at drain. **Drain** (required before `git reset`, `git clean`, Step 4 Finalize, or returning to planning): message `HOLD` to every worker with a task in flight, wait for each `HELD:`, then tell every worker no further tasks are coming so each finishes and auto-terminates. Merge every branch whose work you keep per `<integration-gate>`, then for each package `remove-worktree \"[WORKER_WORKTREE]\"` and `git branch -D \"[WORKER_BRANCH]\"`. When draining to return to planning, carry the `HELD:` summaries and the triggering `BLOCKED` body into `./plan.md` as live context; delete the branches unmerged. Never repurpose a dormant worker for an unrelated package — its context is stale; spawn a fresh one instead. **Refresh a worker** after roughly 6 tasks, or sooner when a report contradicts or forgets an earlier constraint: retire it, spawn a fresh one for the same package on the same worktree and branch, and inline the current state (what's done, what's queued, live contracts) into its first task.\n",
  antigravity: "Workers stay live and idle between waves; stop one (`{\"type\": \"shutdown_request\"}`) only when its package is out of scope for the rest of the card, or at drain. **Drain** (required before `git reset`, `git clean`, Step 4 Finalize, or returning to planning): DM `HOLD` to every worker with a task in flight, wait for each `HELD:`, then `shutdown_request` every worker. Merge every branch whose work you keep per `<integration-gate>`, then for each package `remove-worktree \"[WORKER_WORKTREE]\"` and `git branch -D \"[WORKER_BRANCH]\"`. When draining to return to planning, carry the `HELD:` summaries and the triggering `BLOCKED` body into `./plan.md` as live context; delete the branches unmerged. Never repurpose an idle worker for an unrelated package — its context is stale; spawn a fresh one instead. **Refresh a worker** after roughly 6 tasks, or sooner when a report contradicts or forgets an earlier constraint: stop it, dispatch a fresh one for the same package on the same worktree and branch, and inline the current state (what's done, what's queued, live contracts) into its first task.\n"
}) %>
</lifecycle>

<%= it.variant({
  claude: "<model-selection>\n",
  codex: "<effort-selection>\n",
  opencode: "<effort-selection>\n",
  antigravity: "<model-selection>\n"
}) %>
<%= it.variant({
  claude: "`[MODEL]` defaults to the model that reliably handles most package work. Escalate to the strongest available model — either the package worker itself, or a one-off task dispatch per `<dispatch>` — only for system-level or cross-cutting work where early decisions shape the rest, never as the roster default. A lighter, cheaper model fits a package that is bounded, low-ambiguity, single-component work.\n",
  codex: "`[EFFORT]` defaults to `standard` — the tier that reliably handles most package work. Escalate to `deep` — either the package worker itself, or a one-off task dispatch per `<dispatch>` — only for system-level or cross-cutting work where early decisions shape the rest, never as the roster default. `light` fits a package that is bounded, low-ambiguity, single-component work.\n",
  opencode: "`[EFFORT]` defaults to `standard` — the tier that reliably handles most package work. Escalate to `deep` — either the package worker itself, or a one-off task dispatch per `<dispatch>` — only for system-level or cross-cutting work where early decisions shape the rest, never as the roster default. `light` fits a package that is bounded, low-ambiguity, single-component work.\n",
  antigravity: "`[MODEL]` defaults to the model that reliably handles most package work. Escalate to the strongest available model — either the package worker itself, or a one-off task dispatch per `<dispatch>` — only for system-level or cross-cutting work where early decisions shape the rest, never as the roster default. A lighter, cheaper model fits a package that is bounded, low-ambiguity, single-component work.\n"
}) %>
<%= it.variant({
  claude: "</model-selection>\n",
  codex: "</effort-selection>\n",
  opencode: "</effort-selection>\n",
  antigravity: "</model-selection>\n"
}) %>
